import RegisterClient from "../../../features/auth/RegisterClient";

// SERVER COMPONENT — delegates client-side form logic to RegisterClient.
export default function RegisterPage() {
  return <RegisterClient />;
}
